"use client";
import type { EventStatus } from "@/types";
// TEMPORARY STUB — real implementation added in Phase 7 (Task 7.1).
// eslint-disable-next-line @typescript-eslint/no-unused-vars
export function RevealButton(_props: { eventId: string; status: EventStatus }) {
  return null;
}
